# --- Core API ---
Django==5.2.1
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.28.0
django-filter==25.2
django-cors-headers==4.9.0

# --- Database / media / static / server ---
psycopg2-binary==2.9.11
dj-database-url==3.1.0
pillow==12.1.1
whitenoise==6.11.0
gunicorn==25.0.3
python-dotenv==1.2.1
requests==2.32.5

# --- Chatbot (RAG: Pinecone + OpenAI via LangChain, HuggingFace embeddings) ---
# These are heavy (pull in torch). The Django API boots without them; the
# /api/chatbot/ endpoint reports 503 until they are installed and configured.
langchain==0.3.27
langchain-community==0.3.27
langchain-openai==0.3.33
langchain-pinecone==0.2.12
pinecone[grpc]==5.4.2
sentence-transformers==3.4.1
pypdf==5.1.0

# Local src/ package (RAG helpers)
-e .
